# Web 服务断线与恢复（FE-047 部分）

本轮沿用“开放系统、无登录／用户／角色／权限管理”前提。连接错误恢复只依赖重新读取开放业务 API，不引入令牌、身份或访问控制。

## 已实现

- 页面初次加载或用户点击“刷新列表”时，重新读取服务能力、模板及文档列表。能力读取失败时暂停上传；模板读取失败时不沿用旧模板列表，并给出错误提示。
- 文档列表读取失败时不保留旧的任务徽标、文档数量和详情／复核工作台，避免旧状态被误认为当前状态；上传和分页暂停，提供“重新连接并刷新”入口。
- 服务恢复后，重新获取能力、模板和当前文档列表，恢复上传和列表交互。普通筛选、分页和轮询仍按各自请求刷新。
- 文档详情读取修订列表失败时显示错误和就地重试；重复选择同一文档时以最后一次选择请求为准，旧响应不能覆盖新响应。
- 复核工作台分别处理解析修订元数据和提取运行读取失败：显式失败提示与重试入口；修订数据不可用时不开放字段候选生成，运行数据不可用时不再无限显示“正在读取”。
- 复核异步操作带文档／修订上下文版本。切换文档或修订后，旧读取结果、错误及写入后的刷新不会覆盖新工作台。字段决定、问题处理和成果确认固定使用操作发起时的提取运行 ID；服务端已受理的写入不会因前端切换而被取消。

## 验证

- `pnpm --dir business-web test`：10/10 通过。
- `pnpm --dir business-web build`：8 个离线资源打包成功。
- `python3 business-web/tests/recovery_browser.py`：Playwright 模拟初次 503、恢复、运行中列表 503、再次恢复，断言上传、旧状态清理和服务元数据重新获取。
- 统计、审计和模板 Playwright 回归均通过；`git diff --check` 通过。
- `python3 business-web/tests/review_recovery_browser.py`：Playwright 连续模拟详情修订列表、解析修订及提取运行 503 后恢复；证据深链和服务断线恢复回归通过。
- `python3 business-web/tests/review_race_browser.py`：浏览器延迟文档 A 的字段决定，在等待中切换文档 B；断言写入只指向 A，完成后不重新加载 B 的提取运行。复核失败重试、证据链接和断线恢复回归再次通过。

## 未完成

FE-047 仍保持未完成：检索、审计等异步面板尚未统一提供同等级的就地重试与竞态保护；本轮只用延迟字段决定覆盖了跨文档写入竞态，其他写操作仍需场景验证。FE-048 的原件丢失、预览失败等资源缺失反馈也未在本轮完成。上述验证使用模拟 API，没有替代真实 Doclib 模型或麒麟／NVIDIA 现场验收。
