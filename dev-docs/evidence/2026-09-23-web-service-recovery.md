# Web 服务断线与恢复（FE-047 部分）

本轮沿用“开放系统、无登录／用户／角色／权限管理”前提。连接错误恢复只依赖重新读取开放业务 API，不引入令牌、身份或访问控制。

## 已实现

- 页面初次加载或用户点击“刷新列表”时，重新读取服务能力、模板及文档列表。能力读取失败时暂停上传；模板读取失败时不沿用旧模板列表，并给出错误提示。
- 文档列表读取失败时不保留旧的任务徽标、文档数量和详情／复核工作台，避免旧状态被误认为当前状态；上传和分页暂停，提供“重新连接并刷新”入口。
- 服务恢复后，重新获取能力、模板和当前文档列表，恢复上传和列表交互。普通筛选、分页和轮询仍按各自请求刷新。

## 验证

- `pnpm --dir business-web test`：10/10 通过。
- `pnpm --dir business-web build`：8 个离线资源打包成功。
- `python3 business-web/tests/recovery_browser.py`：Playwright 模拟初次 503、恢复、运行中列表 503、再次恢复，断言上传、旧状态清理和服务元数据重新获取。
- 统计、审计和模板 Playwright 回归均通过；`git diff --check` 通过。

## 未完成

FE-047 仍保持未完成：修订／复核、检索、审计等异步面板尚未统一提供失败后的就地重试与并发请求保护。FE-048 的原件丢失、预览失败等资源缺失反馈也未在本轮完成。上述验证使用模拟 API，没有替代真实 Doclib 模型或麒麟／NVIDIA 现场验收。
