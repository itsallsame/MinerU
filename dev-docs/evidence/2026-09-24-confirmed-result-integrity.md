# 已确认成果版本的内容完整性（2026-09-24）

范围：P3 `BE-057` 的“不可静默覆盖”边界。成果版本已追加保存字段 JSON 与 SHA-256，但详情、历史列表原先仅解析 JSON，下一版确认也只比较上一版摘要。若字段 JSON 或摘要单独损坏，Web/自有 Skill 通过统一业务 API 可能拿到失真的“已确认成果”。

现在业务存储在单版详情、版本列表读取时核对字段 JSON 的 SHA-256；确认新版本前核对该提取运行的**全部既有版本**，而非只看最新版。任何一版内容与摘要不符时，详情/列表 API 返回 409，不输出损坏字段；确认事务拒绝新增版本。原有显式复核、同修订冻结证据、阻断问题和不可变版本号规则保持不变。系统仍无登录、用户、角色或权限管理；模型仍由宿主目录独立提供。

验证：测试分别只改字段 JSON 或只改摘要，检查直接读取、API 详情/列表和再次确认均失败且不泄露伪造字段；另测试较旧版本损坏也阻止创建更高版本，未损坏的另一版本仍可独立读取。定向 `test_review_results.py` 12 项通过；最终 Mac 完整业务回归 342 通过、2 跳过、2 个第三方依赖警告。Ruff lint、feature-list JSON 与 diff 检查通过。无数据库 schema 变化。

限制：这能发现字段内容与已保存摘要不一致，不能防止能同时改写两者的数据库管理员，也不是外部签名或审计见证。真实四类标注样本、隔离麒麟 x86_64+NVIDIA 目标机与回退演练仍未完成。
