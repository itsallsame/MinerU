# 复核工作台：对比修订的过期错误隔离

日期：2026-09-24。分支：`master`。环境：macOS 本机 Chromium；浏览器业务 API 为模拟响应。

问题：逐页对比与“两版此页”读取已经在成功响应后核对当前对比修订，但旧请求若失败，会被通用操作错误处理显示到用户刚切换的新对比下。这样会把过期故障误归给当前选择。

修复：对比请求及页读取的失败也核对修订上下文；上下文已变时丢弃旧错误，仍让当前对比的真实错误沿原有工作台错误反馈呈现。未更改 API、持久数据或模型调用。

验证：`cd business-web && pnpm test && pnpm build` 的 11 项 Node 单测与 8 个离线资源构建通过；`pnpm test:browser` 的 19 项 Chromium 回归通过。新增场景分别延迟旧对比请求与旧页读取并返回 503，切换修订后断言不展示旧故障或旧页文本；同一修订的当前失败仍显示错误，重试后恢复。`.venv/bin/python -m ruff check business-web/tests/revision_diff_race_browser.py` 通过；`git diff --check` 通过。浏览器模拟响应不能替代真实业务 API、实际文档或目标麒麟测试。回滚只涉及前端错误过滤与对应测试，不影响数据。
