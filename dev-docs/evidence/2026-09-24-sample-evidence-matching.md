# 四类样本评估重复值证据匹配（2026-09-24）

范围：`scripts/evaluate_business_samples.py` 的字段候选与冻结证据指标。真实四类人工标注样本仍未取得；本次只验证合成和业务 API 契约，不声称解析准确率或生产验收通过。

- 修改前：同字段同值的首个机器候选若无有效冻结证据，即使后续候选有证据，`evidence_hits` 仍记为 0。新增回归先证明了这一误差。
- 修改后：对同字段同值候选建立一对一匹配，优先最大化有效冻结证据命中，再将剩余人工标注与未使用的同值候选匹配。既不会重复使用同一个候选，也不会因标注/候选顺序而低估可验证的命中数。
- 有效证据仍要求冻结片段 SHA-256 正确、包含字段值，并满足人工指定的原文片段和页码；不放宽身份校验或泄漏标注原文。
- 验证：目标评估测试 8 passed；完整 Mac 业务测试 213 passed、1 skipped；Ruff 通过。

边界：机器候选的精确匹配指标不是人工复核后的最终成果质量。公文、论文、研报、报纸真实脱敏样本、手写/跨页表格/印章等场景，以及隔离麒麟 x86_64 + NVIDIA 模型运行，仍需实测。
