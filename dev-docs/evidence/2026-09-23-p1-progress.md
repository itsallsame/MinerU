# P1 离线底座进度证据

日期：2026-09-23。起点提交：`613cf585`。运行环境：Mac 开发机；**非麒麟、非 NVIDIA、非物理断网**。

已写入：worker Dockerfile、只读模型/清单挂载的 Compose、无网络预检与 SHA-256 清单工具、只读本地模型路径的源码修正、相关测试。
后续补充：容器入口还要求 MinerU Torch 与 vLLM 两个仓库的完整标记；这条检查目前仅静态审查，未在装齐依赖和真实模型的容器内运行。

实际执行：

- `python3 -m pytest -q -o addopts='' --confcutdir=tests/business tests/business/test_offline_configuration.py`：5 passed。
- `docker compose -f docker/compose.business.yaml config --quiet`：设置占位环境变量后通过；仅语法验证，没有启动容器。
- `sh -n docker/worker/entrypoint.sh`、`python3 -m compileall -q scripts/offline_package.py`、`git diff --check`：通过。
- 按仓库默认 pytest 配置运行新测试：5 errors，原因是开发机没有安装 `docvortex`，根 `tests/conftest.py` 在 setup 时导入 MinerU 失败；改用 `--confcutdir=tests/business` 只测独立预检逻辑。
- `uv venv .venv` 使用独立 `UV_CACHE_DIR` 后成功；`uv pip install -e '.[dev,test]' --offline` 未成功，缺少缓存中的 `setuptools>=77.0.3` 和 wheel，且 uv 默认选中了外部 Anaconda 环境。后续需显式 `--python .venv/bin/python` 并准备联网或离线依赖仓库。
- `docker info`：访问本机 daemon 权限被拒；未执行构建或运行测试。

2026-09-23 后续验证：

- 用 `UV_CACHE_DIR=/private/tmp/mineru-uv-cache uv pip install --python .venv/bin/python -e '.[dev,test]'` 安装成功，MinerU 4.0.6、DocVortex 0.4.22 等 124 个包进入仓库专用环境。
- `.venv/bin/python -m pytest -q -o addopts='' tests/unittest/test_models_download_utils.py tests/business/test_offline_configuration.py`：27 passed。加入依赖锁/发布清单测试后，相关三组测试：31 passed。
- `.venv/bin/python -m pytest tests/unittest` 在默认沙箱首次运行结果为 2719 passed、25 failed、51 errors、5 skipped；其中真实 HTTP 服务测试因本机 `socket.bind(127.0.0.1)` 被沙箱拒绝。经授权允许 loopback 后，用 `.venv/bin/python -m pytest -q -o addopts='' --tb=short tests/unittest` 复跑：**2796 passed、4 skipped、0 failed、0 errors**，耗时 233.31 秒。不同跳过数来自测试运行条件变化，未把首次失败隐去。
- Docker daemon 经授权可读；`docker image inspect mineru:4.0.2` 证实已有镜像为 linux/arm64。不能用作麒麟 amd64 的构建证据。
- Mac 上 `uv pip compile pyproject.toml docker/worker/build-requirements.in --extra full --no-emit-package mineru --generate-hashes` 成功产生临时锁，证明命令结构可用；该锁只对应 Mac，未提交、未当作生产依赖。Linux 准备脚本要求基础镜像 Torch/Torchvision/vLLM 精确约束，尚未在目标架构执行。
- 发布清单生成器的纯函数测试覆盖源码/镜像/依赖/模型关联以及 arm64、错误源码标签、缺失 wheel 的拒绝；未在真实 amd64 镜像上执行 CLI。
- 按任务清单中的官方命令 `.venv/bin/python -m pytest tests/business/test_offline_configuration.py tests/business/test_release_manifest.py` 执行：9 passed。`ruff check` 和 `ruff format --check` 对新增/修改的 6 个 Python 文件均通过；两个 shell 脚本分别通过 `sh -n`，Compose 配置语法通过。
- 全量测试产生的 51 字节临时 `:memory:.ses` 已确认并删除，不涉及业务文件。

不能标记通过：30 项 INF 功能整体、镜像 amd64 架构、Linux wheelhouse 锁定、目标模型完备性、Doclib 跨容器通信、物理断网启动、真实样本解析、麒麟 GPU 性能。相关 `feature-list.json` 状态保持 `passes=false`。

下一步：在 Linux amd64 准备机执行锁定依赖与 wheelhouse 制备，导入兼容基础镜像和模型，再于麒麟断网构建/启动并完成容器冒烟；之后做 P1 代码审查和阶段完成报告。

2026-09-23 业务 API 部署骨架补充：

- 新增 `docker/business-api/Dockerfile` 和显式业务 API 进程入口；Compose 现含 `business-api` 与 `doclib-worker` 两服务。API 仅挂业务 SQLite 目录与共享上传目录（可写），worker 仅只读挂共享上传目录、模型与清单；只有开放业务 API 绑定宿主端口，默认 `127.0.0.1:8088`，worker 不发布端口。
- 两个 Dockerfile 将已锁定依赖层放在源码层前面；代码修改不应重建/传输模型权重，但实际镜像层增量和传输大小仍需在目标 Linux 镜像上测量。业务 API 暂共用 NVIDIA/vLLM 基础镜像与 wheelhouse，并不等于它运行时需要 GPU。
- 设置所有必需 Compose 占位变量后，`docker compose -f docker/compose.business.yaml config --quiet` 通过。`.venv/bin/python -m pytest tests/business -q` 在允许本机临时 socket 的环境中为 **52 passed，2 warnings**；`ruff check` 与 `ruff format --check` 对业务代码/测试通过。第一次受限沙箱复跑曾因 socket.bind 被拒出现 1 failed、14 errors；有权限后复跑全绿，不将此归因于代码变更。
- 尚未构建 Linux amd64 API/worker 镜像、验证内部网络/宿主端口映射、量化增量传输、验证物理断网启动；这些 INF/P1 验收项保持未完成。发布清单工具仍只记录 worker 镜像，需补业务 API 镜像标识及发布回退验证。
