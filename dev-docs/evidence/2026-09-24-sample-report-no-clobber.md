# 四类样本评估报告防覆盖（2026-09-24）

范围：`scripts/evaluate_business_samples.py` 的脱敏指标报告写出边界。真实四类人工标注样本仍未取得；本次只验证评估工具行为，不声称解析质量或生产验收通过。

- 修改前回归：同一 `--output` 连续运行两次，第二次返回成功并覆盖第一次报告。
- 修改后：已存在的文件或符号链接直接拒绝；先写同目录临时文件，`fsync` 后以硬链接原子发布，若其他评估进程抢先创建报告则拒绝覆盖，并清理本进程临时文件。
- 回归覆盖已有报告和模拟并发竞争，确认先前报告字节不变且不遗留临时文件。业务 API 仍仅发 GET；原文和人工字段值不进入报告。
- 验证：`tests/business/test_sample_evaluation.py` 6 passed；完整 `tests/business` 211 passed、1 skipped；Ruff 与 `git diff --check` 通过。

边界：以上在 Mac 的合成用例完成。公文、论文、研报、报纸真实脱敏样本及人工标注、隔离麒麟 x86_64 + NVIDIA 上的模型运行与指标仍待实际执行。每次评估应为环境、源码/模型版本与运行时间使用新的报告文件名，并另行记录不可变的发布身份。
